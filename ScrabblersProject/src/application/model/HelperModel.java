package application.model;

import application.algo.WordThread;


public class HelperModel {
/*	
	static WordThread thread;
	static int threadCount = 0;
	
	public static void start(WordThread t) {
		threadCount++;
		if (threadCount > 1) {
			t.end();
		} else {
			thread = t;
		}
	}
	
	public static void close() {
		thread.end();
		threadCount = 0;
	}
	*/
}
